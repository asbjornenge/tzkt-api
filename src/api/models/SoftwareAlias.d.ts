export declare type SoftwareAlias = {
    /**
     * Software version (commit tag)
     */
    version?: string | null;
    /**
     * Date of the commit or when the software was first seen
     */
    date?: string;
};
